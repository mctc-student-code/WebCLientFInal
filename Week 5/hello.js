/* console.log('Hello world!')

let place = "Mckinney"
 let todaysTemp = 75
 console.log("Today's heat index is 107, everone in " +place+ "is dead! the heat index right now is 1000, what is that " + todaysTemp+ " in " +place+ " it's hot in "+ place)
 let TempC= (todaysTemp -32) *5/9 
 console.log(TempC) 

 function shout(text){
    let shouty_text = text.toUpperCase() + '!!!'
    return shouty_text
 }
 console.log(shout('hello world'))
 let message = shout('hello web programmers')
 console.log(message)

 function f_to_c(f, decimalPlaces) {
   let celsius = (f-32) * 5 / 9
   if(decimalPlaces) {
      return celsius.toFixed(decim2alPlaces)
   } else{
      return celsius
   }
 } */


/*  let animals = ['cheetah', 'giraffe', 'lion', 'zebra']

 animals.push('Alligator')
 let AnimalsNameLength = []
 animals.forEach(function(animals ) { 
   let length = animals.length
   AnimalsNameLength.push(length)
 })
 console.log(AnimalsNameLength) */

/*  TempC = f_to_c(todaysTemp, 3)
 console.log(TempC)
 */

 let User = { 
    Username:'Avery',
    Password: 'Avemaria',
    Email: 'AveryEmail@email.com',
    Roles: ['Coder', 'Storyboard Writer' ],
    Contact: {
         PhoneNumber:"123987456", 
         office: 'H5130'
      }
   }
   //WHy is the output different form the teachers |ASK!!
 console.log(User)


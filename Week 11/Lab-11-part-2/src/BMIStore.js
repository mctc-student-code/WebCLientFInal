import {defineStore} from "pinia"
import {ref, computed} from 'vue'
export const useBMIStore = defineStore('bmi', () => {
    

const calcualteBMI = computed:( ( =>) {
const bmi = height.value = 100
return bmi
})


ComputedRef<void> = computed( getter:():number =>)
})